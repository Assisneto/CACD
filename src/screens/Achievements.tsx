import React from "react";
import { View, Text } from "react-native";

const Achievements: React.FC = () => {
  return (
    <View className="flex-1 bg-background justify-center items-center pb-16">
      <Text className="text-textPrimary text-lg">Achievements Screen</Text>
    </View>
  );
};

export default Achievements;
